generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Conversor {
  id            Int    @id @default(autoincrement())
  amount        Float
  fromCurrency  String
  usdValue      Float
  eurValue      Float
}



